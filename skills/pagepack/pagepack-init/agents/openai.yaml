interface:
  display_name: "Pagepack Init"
  short_description: "Create initial .codebase Runtime Docs"
  default_prompt: "Use $pagepack-init to directly create the initial .codebase Runtime Docs when no .codebase/ exists."
