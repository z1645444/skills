interface:
  display_name: "Pagepack Suggest Adapters"
  short_description: "Plan agent adapter patches"
  default_prompt: "Use $pagepack-suggest-adapters to generate a reviewable agent entry patch suggestion."
