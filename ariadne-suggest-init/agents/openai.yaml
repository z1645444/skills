interface:
  display_name: "Ariadne Suggest Init"
  short_description: "Create initial .codebase pack"
  default_prompt: "Use $ariadne-suggest-init to directly create the initial .codebase Codebase Knowledge Pack when no pack exists."
